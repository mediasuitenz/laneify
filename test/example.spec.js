'use strict';

var should = require('chai').should()

describe('Maths', function () {
  describe('adding 2 numbers', function () {
    it('should total correctly', function () {
      var result = 1 + 2
      result.should.equal(3)
    })
  })
})
